apiVersion: v1
kind: Service
metadata:
  name: {{ include "profile.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "profile.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
