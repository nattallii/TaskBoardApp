apiVersion: v1
kind: Service
metadata:
  name: {{ include "board.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "board.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
